<odoo>
    <data>

        <!-- Form -->
        <record id="view_tracked_search_queries_form" model="ir.ui.view">
            <field name="name">Отслеживаемый поисковый запрос</field>
            <field name="model">ozon.tracked_search_queries</field>
            <field name="arch" type="xml">
                <form>
                    <group>
                        <field name="name" />
                    </group>
                </form>
            </field>
        </record>

        <!-- Tree -->
        <record id="view_tracked_search_queries_tree" model="ir.ui.view">
            <field name="name">Отслеживаемые поисковые запросы</field>
            <field name="model">ozon.tracked_search_queries</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name" />
                </tree>
            </field>
        </record>

        <!-- Action -->
        <record model="ir.actions.act_window" id="action_tracked_search_queries">
            <field name="name">Отслеживаемые поисковые запросы</field>
            <field name="res_model">ozon.tracked_search_queries</field>
            <field name="view_mode">tree,form</field>
        </record>

        <!-- Menu -->
        <menuitem name="Отслеживаемые поисковые запросы"
            id="menu_tracked_search_queries"
            parent="ozon.technical"
            action="action_tracked_search_queries"
            sequence="37" />

    </data>
</odoo>